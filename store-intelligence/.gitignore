# Store Intelligence — generated / local-only (also covered by repo-root .gitignore)

__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

# SQLite (Docker volume, validation scripts, local ingest)
*.db
data/*.db
data/phase2_verify.db
data/validation_*.db
validation_*.db

# YOLO weights (auto-downloaded into project root on pipeline run)
yolov8n.pt

# Regenerated pipeline output (optional: commit events.jsonl for reviewers)
# data/events.jsonl
